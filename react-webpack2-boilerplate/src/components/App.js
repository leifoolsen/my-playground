import React from 'react';

const superStyles = {
  backgroundColor: 'green'
};

const App = () => (
  <div style={superStyles}>
    <h1>My (not so) Amazing React App!</h1>
  </div>
);

export default App;
